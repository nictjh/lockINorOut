generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Article {
  id        Int      @id @default(autoincrement())
  title     String
  url       String   @unique
  snippet   String?
  topic     String
  source    String?
  createdAt DateTime @default(now())
}
